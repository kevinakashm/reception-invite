# MP Reception Invitation (Next.js)

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:3000

Files of interest:

- pages/index.js — main UI
- styles/globals.css — styling and emoji animation
